import { DefaultTheme } from "styled-components";

// declaring a Theme with a type of DefaultTheme
const myTheme: DefaultTheme = {
  borderRadius: '5px',
  colors: {
    main: 'red',
    secondary: 'blue'
  }
};

export { myTheme };